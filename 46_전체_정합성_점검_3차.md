# 46. 전체 정합성 점검 3차

> **작성일**: 2026-07-18
> **기준 문서**: [42_전체_정합성_점검_2차](./42_전체_정합성_점검_2차.md) → [43_정합성_리팩토링_실행계획서](./43_정합성_리팩토링_실행계획서.md) 이후 3차 점검
> **설계서**: [docs/superpowers/specs/2026-07-18-consistency-round3-quality-audit-design.md](./docs/superpowers/specs/2026-07-18-consistency-round3-quality-audit-design.md)
> **원자료**: [docs/superpowers/reports/2026-07-18-consistency-round3-*.md](./docs/superpowers/reports/) (baseline·structure·api-surface·doc-consistency·code-rules·tdd-gaps-backend/frontend·regression-check)
> **성격**: 코드가 진실. 실배포(WS-D) 착수 전 코드↔문서 정합·코드 품질(규칙·TDD) baseline 확보.

---

## 1. 결론

42번(2차, 7-02) 이후 ~2주간 대량 머지(에러 envelope·Tier-2 실API·마이페이지 P1~P4·설정/동의·Google OAuth·베타 게이팅 C1/C2a/C2b·웹 이미지·결제 마스터 spec)가 이뤄졌고, **구현이 다시 문서를 앞질렀다**. 3차는 기존 문서 정합 축에 **코드 품질 축(규칙·TDD)을 신설**해 점검했다.

- **43번 회귀**: 42→43 문서 정합 계획은 **실질 종결**(대표 12항목 이행 확인, OPEN 이력 PR 2건 MERGED, 재역행 없음). ✅
- **불일치 진원지 = `04_API_명세서`**: 커뮤니티 설계서(20)는 미구현 기능에 TARGET 라벨을 제대로 달았으나, **04가 그 미래 기능(에스컬레이션·현상금·모더레이션·admin 대량)을 상태 라벨 없이 현재 엔드포인트 표로 나열**해 독자가 구현으로 오인한다. 동시에 42번 이후 머지분(beta·consent·mypage·google·guest)은 04에 **역류 미반영**.
- **코드 품질**: 시크릿 실값 커밋 **0건**(양호). 그러나 (a) `notification-svc`가 gitops 배포 매니페스트에 **누락**(실배포 시 `/notifications/**` 502 위험) (b) `notification-svc` 에러 envelope 미채택 (c) `community/lcs-svc`에 `.omc` 런타임 아티팩트 추적 (d) **jqwik 속성기반 테스트 전무**(전략서 명시 요구 vs 실제 0) (e) 핵심 Service 다수 무테스트.
- **미구현 확정**(테스트 갭 아님): community 에스컬레이션 상태전이·현상금 차감/환불·모더레이션 심각도는 `src/main` grep 0 = **기능 미구현**. 04/02 문서가 이를 존재처럼 표기 → 문서 수정 대상(구현은 별도 기능개발, 본 점검 범위 밖).

**우선순위 요약**: P0 1건(배포 직결) · P1 다수(문서 역류·라벨·규칙·TDD 갭) · P2 이월 다수. 대형 구조 변경은 §8 기록만.

---

## 2. 기준·범위

### 2.1 기준 ref SHA (baseline 원자료)

전 21레포 `git fetch` 후 `origin/develop`(부재 시 `origin/main`) 고정. **점검 종료 시 재확인 결과 13개 코드/문서 레포 전부 SHA 동일 — 점검 중 신규 머지 없음(기준선 유효).**

| 레포 | ref | SHA | 레포 | ref | SHA |
|------|-----|-----|------|-----|-----|
| platform-svc | develop | 4c1ca4f | shared | develop | ff7f5a6 |
| learning-svc | develop | 0461c4f | svc-template | develop | b6c33ee |
| community-svc | develop | 043b830 | gitops | develop | ece471d |
| sandbox-svc | develop | a6d802e | documents | develop | 12fa43b |
| ai-svc | develop | 69d5e0f | frontend | develop | e6be351 |
| lcs-svc | develop | bd64ada | gateway | develop | c8a1282 |
| notification-svc | develop | 0afddfd | | | |

참고 8레포(prototype·storyboard·templates·workflow-dashboard·workflow-guide·.github·home-page·landing-page)는 baseline 원자료 표 참조.

### 2.2 범위·방법

- 4축 병렬 점검(①구조 ②코드/문서 정합성 ③코드 규칙 ④TDD) → 읽기 전용 서브에이전트 원자료 → 컨트롤러 교차 종합·표본 재실행 검증.
- 코드가 진실. 워킹트리 아닌 기준 ref(`git show/ls-tree/grep`)로 실측.
- 제외: `*.zip`·`*.pdf`·`node_modules/`·`build/`·빌드 산출물.

---

## 3. 43번 회귀 체크 (regression-check 원자료)

42번 §3 형식(✅/⚠️/❌/➖). **43번 미이행(❌) 0건.**

| 그룹 | 결과 |
|------|------|
| P0 (6) | P0-1·2·4·5 ✅ 직접검증 / P0-3·6 ⚠️ round2-done 승계 |
| P1 (15) | P1-1·4·5·6·9·13·15 ✅ 직접검증 / 나머지 8건 ⚠️ 승계 |
| P2 (3) | P2-3 ✅ / P2-1·2 ⚠️ 승계 |
| 색인 (4) | I-1·2 ✅ (README·Home 42·43 링크) |
| OPEN 이력 PR | sandbox-svc #18 **MERGED**, .github #6 **MERGED** (메모리 "OPEN"은 lag) |
| 42번 잔여 ⚠️ P0-5 | 조직 프로필 렌더링 = .github#6 MERGED로 종결 ✅ |

**승계 사항**: 43 P1-1은 04_API의 **알림 절(§9)만** 라벨링했다. 3차가 04의 나머지 섹션 라벨 부재를 신규 발견(→ §5 B군). "43 미이행"이 아니라 43 미포함 범위로, 47번 신규 Task로 승계.

---

## 4. 축① 전체 구조 (structure 원자료)

gateway 라우팅은 `application.yml` 선언형 단일 SSoT(RouteLocator 자바설정 없음). 7 svc 컨트롤러 base path ↔ gateway 라우트 ↔ gitops 배포앱 대조 결과:

| # | 발견 | 판정 | 비고 |
|---|------|:----:|------|
| S-1 | **notification-svc가 gitops `apps/`에 없음** (배포앱=admin·web·gateway·platform·learning·community·sandbox·ai·lcs·_migration). gateway엔 `/notifications/**`→8088 라우트 존재, svc 컨트롤러 3종 실재 | **P0** | applicationset `apps/*` 자동발견이라 미배포 → 실배포 시 502. **WS-D 직결** |
| S-2 | platform `/admin`·`/consents`·`/beta` 가 gateway `platform-auth` predicate(`/oauth2,/login,/auth,/users`)에 **미매칭** | **P1** | admin 앱 직결 호출 설계면 정상일 수 있음 — 프론트 실호출 경로 확인 후 라우트 추가 확정 |
| S-3 | notification `/notifications/internal/prefs`가 `/notifications/**` 라우트에 매칭 → 내부 경로 엣지 노출 위험 | **P2** | 타 svc는 `/internal/**`를 base 밖에 둠. 경로 재배치 or predicate 축소(대형은 §8) |
| S-4 | ai `/ai/**`(embed·path·generate·re-engagement)가 gateway 미노출 | **P2** | 내부 전용이면 정상. 03 문서 서술 톤만 정정 |

---

## 5. 축② 코드/문서 불일치 (api-surface·doc-consistency 원자료)

**실측 API 엔드포인트 70개**(platform 16·learning 22·community 11·sandbox 3·ai 7·lcs 6·notification 5). 42번 기준 52 대비 +18. shared Flyway 31개(중앙집중, 7 svc 로컬 0).

### (a) 문서 과소·"미착수"인데 실측 존재 — P1

| # | 문서:위치 | 실측 근거 | 판정 |
|---|-----------|-----------|:----:|
| M-1 | 17_스케줄 §0 "백엔드 52 엔드포인트"·§0.1 svc별 과거 수치(platform 4·알림 device 2) | 실측 70(platform 16·notification 5) | **P1** |
| M-2 | 02_ERD 헤더 "마이그레이션 24파일" | shared db/migration = 31 | **P1** |
| M-3 | platform-svc/README 도메인표 `user`·`github` 2모듈만 | 실측 컨트롤러 8종(beta·consent·avatar·profile·admin allowlist 포함) | **P1** |
| M-4 | 04_API §1·§5·§11 유료 plan/tier 표기 산재 | 결제·plan 미구현(44 "①결제만 미착수") | P2 |

### (b) 문서가 존재 주장하는데 실측 미구현 — P1 (04가 진원지)

| # | 문서:위치 | 실측 근거(grep 0 교차확정) | 판정 |
|---|-----------|----------------------------|:----:|
| M-5 | 04_API 에스컬레이션 `POST /community/questions/{id}/escalate` + 이벤트 표 | community `escalat` src/main 0(RepPoints 상수만) | **P1** |
| M-6 | 04_API §8.2 현상금 `POST .../{id}/bounty` | community `bounty` src/main 0(스키마 컬럼만) | **P1** |
| M-7 | 04_API §8.6·§10.6 모더레이션 `/community/moderation/*`·`/admin/community/sanctions` | community moderation grep 0 | **P1** |
| M-8 | 04_API §10 Admin 대량(~30+: contents·finops·reports·announcements·sandbox 관리) | platform admin 실측 3종(users·approve·allowlist)뿐 | **P1** |
| M-9 | 04_API §5 Sandbox sessions/kill/quotas·§7 멘토 messages/history | sandbox=`/sandbox/run`(SSE)+internal, ai 멘토=`/ai-mentor/sessions` 1종 | **P1** |
| M-10 | 02_ERD §8 study_groups·project_showcases·moderation_queue·user_sanctions | 서비스 로직 미구현(목표 스키마) | P2 |

→ **공통 처방**: 04의 미구현 섹션에 20 설계서 헤더처럼 **TARGET(미구현) 라벨** 부여. 04 전 섹션 상태열 도입은 대형(§8).

### (c) 42번 이후 머지 기능의 04/06 역류 미반영 — P1

| # | 기능(머지) | 미반영 위치 | 판정 |
|---|-----------|-------------|:----:|
| M-11 | 베타 게이팅 C1/C2a/C2b (`/beta/status`·`/admin/allowlist`·`/admin/users/{id}/approve`, web `/beta-pending`) | 04·17·platform README·06 전부 미반영 | **P1** |
| M-12 | 설정/동의(`/consents` 3종)·마이페이지(`/users/me/avatar`·`/profile`)·Google OAuth | 04·06 엔드포인트/화면 미반영(44는 반영됨) | **P1** |
| M-13 | 게스트 진단 4종·재참여 `/ai/re-engagement`·notification prefs 2종 | 04 미반영 | P2 |
| M-14 | 에러 envelope 표준화 현황(notification 미채택) | 04 공통 에러 절에 채택 현황 서술 없음 | P2 |
| M-15 | 결제 PG명 **불일치**: 문서(44·17=토스페이먼츠) ↔ 메모리(PortOne 마스터 spec) | 코드 미구현이라 위배는 아니나 SSoT 미확정 | **P2 (사용자 결정)** |

### (d) 기타 문서

| # | 위치 | 판정 |
|---|------|:----:|
| M-16 | documents README "도메인 서비스 5개" ↔ 실제 7 svc | P2 |
| M-17 | gitops README 구조블록 스테일(`apps/devpath-frontend` 표기, 실제 admin·web·notification 부재) | P2 (S-1과 함께) |

---

## 6. 축③ 코드 규칙 (code-rules 원자료)

- **커밋 컨벤션**: 12레포 위반 0(gitops `deploy(...)` 자동 write-back은 관찰 O-1). **Flyway 네이밍**: shared 31개 전수 준수, 위반 0. **시크릿**: 실값 커밋 **0건**(전건 플레이스홀더·env 참조·k8s secretKeyRef, 컨트롤러 독립 재스캔으로 ghp_/sk-/AKIA/AIza 0 확인). ✅

| # | 위반 | 판정 | 처방 |
|---|------|:----:|------|
| M-18 | notification-svc 에러 envelope **미채택**(전역 핸들러·shared ApiException/ErrorResponse grep 0) | **P1** | `@Import(ApiExceptionHandler.class)` + 도메인예외 ApiException 파생 + envelope 회귀테스트 |
| M-19 | community-svc `.omc/` **7파일 추적**(session json·agent-replay·hud-state 등) | **P1** | `git rm -r --cached .omc/` + `.gitignore` 등록(대화/도구 컨텍스트 노출 우려, 신속) |
| M-20 | lcs-svc `.omc/` **2파일 추적** | **P1** | M-19와 동일 |
| M-21 | 10개 svc/gateway/svc-template `.gitignore`에 `.omc/` 누락 | P2 | 전 레포 `.gitignore` 추가(svc-template 먼저=신규 상속) |
| M-22 | gitops `.gitignore` 자체 부재 | P2 | `.omc/`·`.DS_Store`·에디터 캐시 담은 `.gitignore` 신설 |

관찰: learning-svc는 공용 advice 대신 svc-local `GlobalExceptionHandler`(shared `ErrorResponse` 재사용, envelope는 정합) — §8. ai-svc는 공용 + `OllamaExceptionHandler` 병존 — §8.

---

## 7. 축④ TDD 적합성 (tdd-gaps 원자료)

전략서 11번: 피라미드 Unit75/Int20/E2E5 + jqwik 속성기반 + §2.1 도메인 불변식.

| # | 갭 | 근거 | 판정 | 보강 |
|---|----|------|:----:|------|
| M-23 | **jqwik/@Property 전무**(전 10레포 grep 0) | 전략서 §2.1·§7 명시 요구 vs 실제 0 | **P1** | 적응형 난이도 수렴·스트릭 TZ 연속성·피어매칭 대칭성 속성테스트(전략서 v1 목표라 P0 아닌 P1) |
| M-24 | learning 무테스트 Service 6종(Assessment·Claim·GuestAssessment·Content·InternalContent·InternalSimilar) | 동명無 & src/test 참조 0 | **P1** | 도메인 로직 보유분 `*ServiceTest` 신설 |
| M-25 | platform 무테스트 Service 4종(GithubEmailOAuth2·Consent·Account·UserProfile) | 동명無 & 참조 0 | **P1** | OAuth 매핑·동의 로직 우선 `*ServiceTest` |
| M-26 | frontend integration_test E2E 전무 | ls-tree 0(golden_path 위젯테스트가 부분 대체) | **P1** | `apps/web/integration_test/golden_path_test.dart` |
| M-27 | web diagnostic·mypage page 위젯테스트 부재(controller만) | page_test 0 | **P1** | `diagnostic_page_test`·`mypage_page_test` |
| M-28 | 평판 7일 신규계정 제한 테스트 미확인 | ReputationServiceTest 미검출 | P2 | 신규계정 감쇄 케이스 추가 |
| M-29 | @SpringBootTest 상단 편중(community 96%·gateway 100%·learning 71%) | 비중 표 | P2 | 순수 도메인 로직 POJO 단위로 하향 |
| M-30 | dp_core 모델·mobile 서비스·admin controller 소형 테스트 결손 | 갭 표 | P2 | 이월 |

**주의**: community 에스컬레이션·현상금·모더레이션은 "테스트 갭"이 아니라 **기능 미구현**(§5 M-5~M-7, §8). 테스트 신설 대상 아님.

---

## 8. 코드 관찰 — 대형 구조 (기록만, 리팩토링 금지)

안전·소형 리팩토링 범위를 벗어나므로 **기록만** 한다(실배포 직전 안정성 우선).

- **G-1 (04 전 섹션 상태열 도입)**: 04_API의 근본 해법은 전 섹션에 구현/TARGET 상태열을 도입하는 것. 개별 섹션 TARGET 라벨(M-5~M-9)은 P1로 즉시 처리하되, 전면 재구조화는 별도 문서 리팩토링 트랙.
- **G-2 (community 미구현 기능 개발)**: 에스컬레이션 상태전이·현상금 차감/환불(optimistic locking)·모더레이션 심각도는 기능 개발 과제(설계는 20번 존재). 본 점검은 문서 라벨만 정정.
- **G-3 (에러 핸들러 수렴)**: learning 독자 advice·ai Ollama advice 병존 → 공용 `ApiExceptionHandler` 수렴 여부는 API 시그니처/동작 영향 가능성 있어 대형.
- **G-4 (notification internal 경로 재배치)**: `/notifications/internal/prefs` → `/internal/notifications/prefs`는 라우팅·호출처 동시 변경(대형).
- **G-5 (엔드포인트 총계 SSoT)**: 42번 "52"가 여러 문서에 확산. 70으로 갱신하되 단일 SSoT(17 §0) 지정 권장.

---

## 9. 위생 (code-rules 위생 표 + documents 방치)

- **`.omc/` 추적**: community-svc 7파일·lcs-svc 2파일 (M-19·M-20, P1). frontend·shared만 `.gitignore`에 `.omc/` 보유, 나머지 10레포 누락(M-21, P2). gitops `.gitignore` 부재(M-22, P2).
- **build/·.gradle/·node_modules/ 추적**: 전 레포 0건(양호).
- **documents 방치 3파일**(워킹트리 untracked, 점검 기준 외):
  - `docs/superpowers/plans/2026-06-24-landing-validation-test-plan.md` + `docs/superpowers/specs/2026-06-24-office-hours-landing-validation-design.md` — superpowers 산출물. **처리안**: 본 정합성 브랜치 또는 별도 `docs/*` 브랜치로 커밋(내용 유효).
  - `.tier1-baseline.md` — Tier-1 기준선 스냅샷. **처리안**: 내용 확인 후 `docs/`로 이관하거나, 일회성 작업 아티팩트면 삭제(47번 실행 시 내용 요약 후 판단).

---

## 부록: 우선순위 색인 (47번 입력)

- **P0 (1)**: S-1(notification gitops 배포 누락) — WS-D 직결.
- **P1 문서 (12)**: M-1·2·3(수치·모듈) · M-5·6·7·8·9(04 미구현 TARGET 라벨) · M-11·12(머지 역류) · S-2(gateway 라우팅) · M-16 유사.
- **P1 규칙 (3)**: M-18(notification envelope) · M-19·20(.omc 추적).
- **P1 TDD (5)**: M-23(jqwik) · M-24·25(무테스트 Service) · M-26·27(frontend).
- **P2 (이월 다수)**: M-4·10·13·14·15·17·21·22·28·29·30 · S-3·4.
- **사용자 결정**: M-15(결제 PG SSoT: PortOne vs 토스페이먼츠).
- **대형(기록만)**: G-1~G-5.
