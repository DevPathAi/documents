# 47. 품질 리팩토링 실행계획서

> **작성일**: 2026-07-18
> **기준 문서**: [46_전체_정합성_점검_3차](./46_전체_정합성_점검_3차.md)
> **목적**: 46번의 P0·P1 불일치·갭·규칙위반을 **안전·소형 리팩토링 Task**로 실행한다. 대형 구조 변경(46 §8 G-1~G-5)은 Task화하지 않는다.

---

## 실행 규칙

1. **레포별 `develop`에서 분기**: 문서 `docs/*`, 위생·규칙 `chore/*`, 리팩토링 `refactor/*`, 테스트 보강 `test/*`. PR → **CI 녹색 후** merge commit. `main`·`develop` 직접 푸시 금지.
2. **코드 변경 Task는 테스트 선행**: 변경 대상에 테스트가 없으면 먼저 현재 동작 고정 테스트를 작성(신규 검증은 실패 확인 후 구현 — TDD). 통과 상태에서 리팩토링.
3. **동작 불변**: 리팩토링 전후 동작 변경 없음. 허용=데드코드·중복 제거·규칙 위반 수정·명명 정리·누락 테스트 보강·위생. 금지=모듈 경계 재조정·API 시그니처 변경(→46 §8 기록만).
4. **shared 컨트랙트 변경 시** `gh workflow run publish.yml --ref develop` 수동 발행.
5. **서브에이전트 위임**: 각 Task 명세(레포·브랜치·파일·내용·검증)를 그대로 전달(SCOPE_LOCK + 쓰기 허용 변형: 명세 파일 외 수정 금지, 명세 커밋 1개, push·PR까지 후 정지). 컨트롤러가 `git log`·`gh pr diff`·테스트 재실행으로 검증, 인접 레포 스팟체크.
6. **P2는 이월 가능**(§4). P0→P1→P2 순 실행.

---

## P0 — 실배포 직결 (즉시)

### T-01 · notification-svc gitops 배포 매니페스트 추가 (46 S-1)

- **레포/브랜치**: `devpath-gitops` / `feat/notification-svc-deploy`
- **문제**: `apps/`에 devpath-notification-svc 부재 → applicationset `apps/*` 자동발견에서 제외 → 실배포 시 gateway `/notifications/**` → 502.
- **파일**(기존 svc 매니페스트를 템플릿으로, 예: `apps/devpath-lcs-svc/base/` 복제):
  - `apps/devpath-notification-svc/base/deployment.yaml` (image=ghcr `:sha`, port 8088, env=DB/Kafka/Redis, secretKeyRef 필요분)
  - `apps/devpath-notification-svc/base/service.yaml` (8088)
  - `apps/devpath-notification-svc/base/kustomization.yaml`
  - 필요 시 `apps/devpath-notification-svc/overlays/*` (타 svc 구조 동일 계승)
- **검증**: `kustomize build apps/devpath-notification-svc/base` 성공. `gh pr diff`에 notification 외 변경 없음. applicationset이 명시목록이면 목록에 추가.
- **주의**: applicationset revision=**main**. develop 머지 후 실배포는 develop→main 릴리스에서 반영. WS-D 착수 전 필수.
- **PR 제목**: `feat: notification-svc 배포 매니페스트 추가 (정합성 3차 S-1)`

---

## P1 — 문서 (documents 단일 브랜치 `docs/consistency-round3-fixes`, 항목별 커밋)

> documents 항목은 상호 독립이므로 한 브랜치에 항목 단위 커밋으로 묶는다(43번 관례).

### T-02 · 04_API 미구현 섹션 TARGET 라벨 (46 M-5~M-9) ★진원지

- **파일**: `documents/04_API_명세서.md`
- **수정**: 아래 섹션 각 표/헤더에 **`상태: TARGET(미구현)`** 라벨 명시(§9 알림 절의 기존 라벨 형식 계승):
  - 에스컬레이션 `POST /community/questions/{id}/escalate` 및 관련 이벤트 표 (M-5)
  - §8.2 현상금 `POST .../{id}/bounty` (M-6)
  - §8.6·§10.6 모더레이션 `/community/moderation/*`·`/admin/community/sanctions` (M-7)
  - §10 Admin 대량(contents·finops·reports·announcements·sandbox 관리): "구현=users·approve·allowlist 3종, 그 외 TARGET" 상태열 추가 (M-8)
  - §5 Sandbox sessions/kill/quotas·§7 멘토 messages/history: 실측 계약(`/sandbox/run` SSE·`/ai-mentor/sessions`)으로 정정 또는 TARGET (M-9)
- **완료 기준**: 위 5개 섹션에 TARGET/구현 라벨 존재, 실측 70개(api-surface)와 모순 없음.
- **커밋**: `docs(04): 미구현 커뮤니티/admin/sandbox/mentor 엔드포인트 TARGET 라벨 (46 M-5~9)`

### T-03 · 04_API·06 머지 기능 역류 반영 (46 M-11·M-12)

- **파일**: `documents/04_API_명세서.md`, `documents/06_화면_기능_정의서.md`
- **수정**: 42번 이후 머지분을 04 엔드포인트 표·06 화면에 추가:
  - 베타 게이팅: `GET /beta/status`·`POST /admin/allowlist`·`POST /admin/users/{id}/approve` + 06 `/beta-pending` 화면 (M-11)
  - 설정/동의 `/consents` 3종·마이페이지 `/users/me/avatar`·`GET/PUT /users/me/profile`·`DELETE /users/me` + 06 `/consent`·`/mypage`·`/settings` 화면, Google OAuth (M-12)
- **완료 기준**: 04에 위 엔드포인트 행 존재, 06에 화면 존재.
- **커밋**: `docs(04,06): 베타게이팅·마이페이지·동의·구글OAuth 역류 반영 (46 M-11,12)`

### T-04 · 17_스케줄 총계·svc별 수치 갱신 (46 M-1)

- **파일**: `documents/17_스케줄.md`
- **수정**: §0 "52 엔드포인트" → **70**, §0.1 svc별 수치를 실측(platform 16·learning 22·community 11·sandbox 3·ai 7·lcs 6·notification 5)으로. 증가분 출처 1줄(beta·consent·avatar/profile·re-engagement·guest).
- **완료 기준**: "52" 잔존 0, svc별 수치가 api-surface와 일치.
- **커밋**: `docs(17): 백엔드 엔드포인트 총계 70·svc별 수치 갱신 (46 M-1)`

### T-05 · 02_ERD 마이그레이션 수 정정 (46 M-2)

- **파일**: `documents/02_ERD_문서.md`
- **수정**: 헤더 "마이그레이션 24파일" → **"31파일(shared 중앙집중)"**.
- **완료 기준**: "24파일" 잔존 0.
- **커밋**: `docs(02): Flyway 마이그레이션 31파일로 정정 (46 M-2)`

### T-06 · README 도메인 서비스 수 정정 (46 M-16)

- **파일**: `documents/README.md`
- **수정**: "도메인 서비스 5개" → **7개**(platform·learning·community·sandbox·ai·lcs·notification).
- **완료 기준**: "5개" 잔존 0.
- **커밋**: `docs(README): 백엔드 도메인 서비스 7개로 정정 (46 M-16)`

## P1 — 서비스 레포 README (레포별 별도 브랜치·PR)

### T-07 · platform-svc README 모듈 반영 (46 M-3)

- **레포/브랜치**: `devpath-platform-svc` / `docs/readme-modules`
- **파일**: `devpath-platform-svc/README.md`
- **수정**: 도메인표에 beta 게이팅(AdminUser·BetaStatus)·consent·mypage(avatar/profile)·admin allowlist 모듈 추가, OAuth "GitHub·Google" 반영.
- **완료 기준**: README 모듈표가 실측 컨트롤러 8종과 정합.
- **PR 제목**: `docs: README 도메인 모듈 실측 반영 (정합성 3차 M-3)`

---

## P1 — 규칙·위생 (레포별 별도 브랜치·PR)

### T-08 · notification-svc 에러 envelope 채택 (46 M-18) [코드·테스트 선행]

- **레포/브랜치**: `devpath-notification-svc` / `refactor/error-envelope`
- **테스트 선행**: 현재 예외 응답 형태를 고정하는 컨트롤러 테스트 작성(현행 동작 캡처) → 실패 확인 없이 통과 상태 확보.
- **수정**: `NotificationApplication`에 `@Import(ApiExceptionHandler.class)` 추가. 도메인 예외를 shared `ApiException` 파생으로 정리. envelope 회귀 테스트 추가(타 svc `*ControllerEnvelopeTest` 형식 계승).
- **검증**: `./gradlew test` 통과(로컬 도커 부재 시 `--tests '*Envelope*'` + CI). shared 사용이므로 shared 컨트랙트 변경은 없음(소비만).
- **완료 기준**: `grep -nE 'ApiExceptionHandler'` 히트, envelope 테스트 그린.
- **PR 제목**: `refactor: shared 에러 envelope 채택 (정합성 3차 M-18)`

### T-09 · community-svc .omc 추적 제거 (46 M-19)

- **레포/브랜치**: `devpath-community-svc` / `chore/omc-hygiene`
- **수정**: `git rm -r --cached .omc` + `.gitignore`에 `.omc/` 추가(frontend/shared 선례).
- **검증**: `gh pr diff`에 `.omc` 외 변경 0. CI 녹색(테스트 CI 없으면 로컬 `./gradlew test` 1회).
- **PR 제목**: `chore: .omc 도구 상태 추적 제거·gitignore 등록 (정합성 3차 M-19)`

### T-10 · lcs-svc .omc 추적 제거 (46 M-20)

- **레포/브랜치**: `devpath-lcs-svc` / `chore/omc-hygiene`
- **수정·검증·PR**: T-09와 동일 형식(`.omc/` 2파일).
- **PR 제목**: `chore: .omc 추적 제거·gitignore 등록 (정합성 3차 M-20)`

---

## P1 — TDD 보강 (레포별 `test/*` 브랜치)

### T-11 · learning-svc 무테스트 Service 테스트 (46 M-24) [테스트 신설]

- **레포/브랜치**: `devpath-learning-svc` / `test/service-coverage`
- **선행 확인**: 대상 6종(Assessment·Claim·GuestAssessment·Content·InternalContent·InternalSimilar)을 `git show`로 열어 **도메인 로직 보유분 우선**(얇은 위임/CRUD는 스모크 최소 또는 이월).
- **수정**: 로직 보유 Service에 `*ServiceTest`(순수 단위, @SpringBootTest 지양) 신설.
- **검증**: `./gradlew test --tests '*ServiceTest'` 그린.
- **PR 제목**: `test: learning 서비스 단위 테스트 보강 (정합성 3차 M-24)`

### T-12 · platform-svc 무테스트 Service 테스트 (46 M-25) [테스트 신설]

- **레포/브랜치**: `devpath-platform-svc` / `test/service-coverage`
- **수정**: GithubEmailOAuth2UserService(OAuth 사용자매핑)·ConsentService(동의 로직)·AccountService·UserProfileService 단위 테스트. OAuth 매핑·동의 로직 우선.
- **검증**: `./gradlew test` 그린.
- **PR 제목**: `test: platform 서비스 단위 테스트 보강 (정합성 3차 M-25)`

### T-13 · frontend integration_test + page 위젯 테스트 (46 M-26·M-27) [테스트 신설]

- **레포/브랜치**: `devpath-frontend` / `test/web-coverage`
- **수정**:
  - `apps/web/integration_test/golden_path_test.dart` (온보딩→path SSE→sandbox 골든패스) (M-26)
  - `apps/web/test/features/diagnostic/diagnostic_page_test.dart`·`.../mypage/mypage_page_test.dart` (M-27)
- **검증**: `melos run test` + `melos run format`(format 게이트 별도) + `melos run analyze` 그린. Riverpod 3.0 인라인 `ProviderContainer(overrides:[...])` 패턴 준수.
- **PR 제목**: `test: web integration_test·diagnostic/mypage 위젯 테스트 (정합성 3차 M-26,27)`

### T-14 · jqwik 속성기반 테스트 도입 (46 M-23) [테스트 신설]

- **레포/브랜치**: `devpath-learning-svc` / `test/jqwik-properties` (핵심 불변식 보유 레포부터)
- **선행**: `build.gradle.kts`에 jqwik 의존 추가(테스트 스코프).
- **수정**: 적응형 난이도 수렴(+0.1/-0.05, [0,1]) `AdaptiveEnginePropertyTest`, 스트릭 TZ 연속성 속성 테스트. (피어매칭 대칭성은 구현 존재 확인 후 — 미구현이면 이월.)
- **검증**: `./gradlew test --tests '*PropertyTest'` 그린.
- **주의**: 전략서 v1 목표 항목. 핵심 2~3개 불변식으로 착수(전면 도입은 점진).
- **PR 제목**: `test: jqwik 속성기반 테스트 도입(적응형 난이도·스트릭) (정합성 3차 M-23)`

---

## P1 — 라우팅 확인 후 조건부 (46 S-2)

### T-15 · platform admin/consents/beta gateway 라우팅 (46 S-2) [확인 선행]

- **선행 확인**: 프론트(web·admin)가 `/admin`·`/consents`·`/beta`를 **gateway 경유로 호출하는지 vs platform 직결**인지 확인(frontend api_config·admin api base). 직결이면 정상(→ 이월·문서 주석), gateway 경유면 아래 실행.
- **레포/브랜치**(경유 확정 시): `devpath-gateway` / `fix/platform-routes`
- **수정**: `application.yml` platform-auth predicate에 `/admin/**,/consents/**,/beta/**` 추가.
- **검증**: gateway 테스트(라우트 predicate) 그린.
- **PR 제목**: `fix: gateway platform admin/consents/beta 라우트 추가 (정합성 3차 S-2)`

---

## P2 — 이월 (다음 라운드 또는 여력 시)

| 항목 | 내용 | 근거 |
|------|------|------|
| M-4 | 04 유료 plan 표기에 "결제 미구현(44 ①)" 주석 | 46 §5 |
| M-10 | 02_ERD 커뮤니티 목표 스키마에 "로직 미구현(TARGET)" 헤더 주석 | 46 §5 |
| M-13 | 04에 게스트 진단·re-engagement·notification prefs 반영 | 46 §5 |
| M-14 | 04 공통 에러 절에 envelope 채택 현황(notification 미채택→T-08 후 갱신) | 46 §5 |
| M-17 | gitops README 구조블록 스테일 정정(T-01과 함께 가능) | 46 §5 |
| M-21 | 10개 레포 `.gitignore`에 `.omc/` 추가(svc-template 먼저) | 46 §6 |
| M-22 | gitops `.gitignore` 신설 | 46 §6 |
| M-28 | 평판 7일 신규계정 제한 테스트 | 46 §7 |
| M-29 | @SpringBootTest 편중 하향(POJO 단위 이동) | 46 §7 |
| M-30 | dp_core 모델·mobile 서비스·admin controller 소형 테스트 | 46 §7 |
| S-3·S-4 | notification internal 엣지 노출·ai `/ai/**` 문서 톤(대형은 G-4) | 46 §4 |

---

## 사용자 결정 필요

- **M-15 · 결제 PG SSoT**: 문서(44·17=토스페이먼츠) ↔ 메모리(PortOne 마스터 spec, platform #28). 코드 미구현이라 위배는 아니나 착수 전 확정 필요. **결정 후** 44·17·27 문구를 SSoT로 통일하는 Task 추가.

---

## 셀프 리뷰 — 46 P0/P1 ↔ 47 Task 1:1 대조

| 46 항목 | 우선순위 | 47 Task | 커버 |
|---------|:--------:|---------|:----:|
| S-1 notification 배포 누락 | P0 | T-01 | ✅ |
| S-2 gateway platform 라우팅 | P1 | T-15(확인 선행) | ✅ |
| M-1 17 총계·수치 | P1 | T-04 | ✅ |
| M-2 02 마이그레이션 수 | P1 | T-05 | ✅ |
| M-3 platform README 모듈 | P1 | T-07 | ✅ |
| M-5·6·7·8·9 04 미구현 TARGET | P1 | T-02 | ✅ |
| M-11·12 04·06 머지 역류 | P1 | T-03 | ✅ |
| M-16 README 도메인 수 | P1 | T-06 | ✅ |
| M-18 notification envelope | P1 | T-08 | ✅ |
| M-19 community .omc | P1 | T-09 | ✅ |
| M-20 lcs .omc | P1 | T-10 | ✅ |
| M-23 jqwik | P1 | T-14 | ✅ |
| M-24 learning 무테스트 Service | P1 | T-11 | ✅ |
| M-25 platform 무테스트 Service | P1 | T-12 | ✅ |
| M-26·27 frontend 테스트 | P1 | T-13 | ✅ |
| M-4·10·13·14·17·21·22·28·29·30, S-3·4 | P2 | 이월 표 | ✅ |
| M-15 결제 PG | 사용자결정 | 결정 후 Task | ⏸ |
| G-1~G-5 | 대형 | Task화 금지(46 §8) | ➖ |

**누락 0**: 46의 P0·P1 전 항목이 47 Task로 존재. placeholder(TBD·"적절히") 없음. 코드 Task(T-08·11·12·13·14)는 테스트 선행 명시.
