# 48. 동시성 제어 관례

이 문서의 모든 항목은 **측정 결과이거나 이 프로젝트가 실제로 겪은 사고**다. 일반론은 넣지 않았다.

근거: `docs/superpowers/specs/2026-08-21-community-concurrency-control-design.md`
(devpath-community-svc 의 경쟁 다섯 건을 닫으며 정한 방식)

---

## 1. 집의 방식은 "DB 가 심판"

착수 전 아홉 개 레포를 전수 조사했다.

| 측정 | 결과 |
|---|---|
| `@Version` · `@Lock` · `LockModeType` 사용처 | **0 건** |
| `DataIntegrityViolationException` 을 잡는 서비스 | **6 개** |
| 격리 수준 설정 | 없음 → **READ COMMITTED**(PostgreSQL 기본) |

즉 애플리케이션 레벨 락을 쓰지 않고, **유일성·원자성 판정을 데이터베이스에 맡긴다.**
새 동시성 문제를 만나면 이 방향을 먼저 시도한다.

---

## 2. ★`@Transactional` 안에서 `DataIntegrityViolationException` 을 잡지 말 것★

잡으면 트랜잭션이 **rollback-only 로 마킹돼 커밋 시점에 다시 터진다.** 이 프로젝트가 광고
기능에서 겪었고 `community-svc` 의 `ReportService` 주석에 남아 있다.

따라서 **트랜잭션 안에서는 예외를 던지지 않는 수단만 쓴다**:

- 행 락 `SELECT ... FOR UPDATE`
- 조건부 `UPDATE` + 영향 행 수 판정
- `INSERT ... ON CONFLICT DO UPDATE`

유니크 제약 + 예외 포착은 **트랜잭션 밖**에서만 성립한다(`ReportService.create` 가 그래서
`@Transactional` 을 붙이지 않는다).

---

## 3. ★"쓰기가 알아서 락을 잡아 준다" 는 기대는 커밋 전 어느 시점에도 성립하지 않는다★

가장 값비싸게 배운 것이다. 처음에는 "락이 없어도 두 번째 요청은 자기 `UPDATE` 에서 막힌다"
고 보고 그 관측에 판별력이 없다고 판단했다. **반대였다** — 락이 없으면 아예 막히지 않는다.

- `save()` 는 엔티티를 더티로 만들 뿐 SQL 을 내지 않는다
- 뒤이어 JPQL 질의가 있어도 ★Hibernate 의 AUTO flush 는 **테이블 범위로 판단**★한다.
  질의 대상 테이블과 더티 엔티티의 테이블이 겹치지 않으면 flush 를 건너뛴다
- 그래서 `UPDATE` 는 커밋까지 미뤄지고 그동안 행 락이 하나도 없다

실측: 관리자 내리기 트랜잭션을 연 채 투표를 던졌더니 **막히지도 않고 500ms 안에 성공**했다.
명시적 `FOR UPDATE` 만이 그 시점에 락을 잡는다.

---

## 4. 언제 무엇을

| 상황 | 수단 |
|---|---|
| 중복 삽입을 막는다 | 유니크 제약 (포착은 트랜잭션 **밖**) |
| **한 행**에 대한 read-modify-write 를 직렬화한다 | `SELECT ... FOR UPDATE` |
| **여러 진입점**이 같은 집계 행으로 몰린다 | `INSERT ... ON CONFLICT DO UPDATE SET x = t.x + EXCLUDED.x` |
| 단일 문장으로 표현된다 | 조건부 `UPDATE` + 영향 행 수 판정 |

★**행 락은 "같은 행"에 대한 경쟁만 직렬화한다**★ — 당연해 보이지만 놓치기 쉽다.
community-svc 에서 콘텐츠 행 락으로 네 경쟁을 닫았는데, **같은 작성자의 서로 다른 콘텐츠**에서
같은 평판 행으로 몰리는 경쟁은 잠글 공통 행이 없어(첫 요청 시점에는 그 행이 아직 없기까지 하다)
그대로 남았다. 그건 원자적 UPSERT 로 따로 닫아야 했다.

★**find-then-save 는 두 가지로 깨진다**★ — 행이 없으면 둘 다 INSERT 해 PK 를 위반하고(→ §2 트랩
→ 500), 행이 있으면 둘 다 옛 값을 읽어 나중 것이 앞의 갱신을 덮는다. 후자가 더 조용해서 더 나쁘다.
**"행을 미리 seed 만 하고 JPA 로 갱신" 은 앞의 것만 고치고 뒤의 것을 남기므로 답이 아니다.**

**JPA 를 우회하면 캐시가 낡는다.** 네이티브 UPSERT 로 바꿀 때
`@Modifying(flushAutomatically = true, clearAutomatically = true)` 가 필요하다 — 같은 트랜잭션에서
이미 로드한 엔티티가 옛 값을 들고 있고, 그 값을 읽는 코드가 있다(우리는 배지 판정이 읽었다).
대가로 영속성 컨텍스트 **전체**가 비므로, **평판 호출 뒤에 앞서 로드한 엔티티를 `save` 하는 곳이
없는지 메서드 단위로 확인**하고 전체 스위트로 재확인한다.

---

## 5. 락 순서와 타임아웃

- **명시적 락은 직접 대상 한 행만** 잡는다. 이어지는 갱신 순서를 레포 문서에 고정한다
  (community-svc: **답변 → 질문**, 그리고 답변 락을 쥔 뒤에만 글을 잠근다).
- ★**락 대기는 반드시 유한**해야 한다★. 무한 대기는 커넥션을 쌓아 서비스를 죽인다.
- ★**통념 정정**: `jakarta.persistence.lock.timeout` 힌트는 **PostgreSQL 17 + Hibernate 에서
  실제로 먹는다**★. "Postgres 는 `NOWAIT`/`SKIP LOCKED` 만 지원하니 힌트가 무시된다" 는 말이
  돌지만, 대조군을 세워 재 보니 아니었다 — 세션 기본 `lock_timeout` 이 `0`(무한)이고 롤 설정도
  비어 있는 상태에서, 힌트를 단 조회가 약 3 초에
  `canceling statement due to lock timeout` 으로 끊겼다.
- **락을 쥐고 도는 코드의 소요도 함께 잰다.** 이벤트 수에 비례하는 코드가 락 안에 있으면
  스케일에 따라 다른 요청이 타임아웃한다(실측: 이벤트 400 건 회수에 397 ms ≈ 이벤트당 1 ms).

---

## 6. 동시성 테스트를 쓰는 법

### 확률적 경쟁을 결정적으로 만든다

★**테스트가 첫 트랜잭션을 직접 열어 쥔 채** 두 번째 요청을 워커 스레드로 던진다.★
반복 실행이나 barrier 없이 매번 같은 결과가 나온다.

```java
tx.executeWithoutResult(st -> {
  contentAdmin.hideAnswer(answerId);        // 락 획득, 아직 커밋 전
  vote = pool.submit(() -> voteService.voteAnswer(v, answerId, 1));
  assertStillInFlight(vote);                // 인터리빙 증거
});                                          // 커밋 → 해제
assertThatThrownBy(() -> vote.get(...)).hasCauseInstanceOf(NotFoundException.class);
assertThat(repOf(answerer)).isZero();       // 락이 있어야만 성립
```

**단언 두 개가 서로 다른 일을 한다.** "아직 진행 중" 은 인터리빙이 실제로 일어났다는 증거이고,
커밋 뒤의 결과는 락의 효과다. 앞의 것이 없으면 두 번째 요청이 커밋 *이후에* 통째로 실행돼도
테스트가 통과한다.

### 제약

- ★**테스트 클래스에 `@Transactional` 을 붙이면 안 된다**★ — 워커 스레드가 커밋된 데이터를
  못 본다. 그래서 **`@BeforeEach` 정리가 필수**다. 전용 id 공간을 정하고 그 범위만 지운다.
- **커넥션 풀 크기를 확인한다.** 이 방식은 동시 커넥션 2 개를 쓴다
  (community-svc 테스트 프로파일은 `maximum-pool-size: 4`).
- 워커가 매달리지 않도록 모든 `get()` 에 타임아웃을 주고 `shutdownNow()` 를 정리에 둔다.

### ★덤으로 닫힌 경쟁은 되돌림으로 증명한다★

하나의 락이 여러 경쟁을 닫으면 나중에 쓴 테스트는 **처음부터 green** 이다. 가드를 잠시 빼서
red 를 관측하지 않으면 "green 인데 아무것도 검증하지 않는 테스트" 가 된다.

**이 되돌림이 실제로 값을 했다** — community-svc 에서 되돌림 관측이 계획에 없던 여섯 번째
경쟁을 찾아냈다. 집계 유실을 보러 갔다가 그 앞에서 `duplicate key ... user_reputation_pkey` 로
죽으며 드러났고, 그것이 §4 의 "행 락은 같은 행만 직렬화한다" 를 가르쳐 줬다.

### ★측정법 유효성을 먼저 세운다★

"충분히 많은 이벤트를 회수하는 데 얼마나 걸리나" 를 재려고 투표자 200 명을 세웠는데
이벤트는 **4 건**이었다 — 일일 획득 상한에 걸려 그 뒤로는 이벤트가 아예 안 쌓였다.
테스트는 green 이었고 아무것도 재지 않았다.

**소요를 재기 전에 측정 대상이 실제로 충분한지 단언한다.** 그리고 ★그 가드를 낮게 잡으면
없느니만 못하다★ — 처음 가드가 `> 3` 이라 4 건짜리 퇴화를 통과시켰다.
