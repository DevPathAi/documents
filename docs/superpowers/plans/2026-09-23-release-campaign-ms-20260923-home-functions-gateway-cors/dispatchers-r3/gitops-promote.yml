---
name: Mission Spine Release Gate Dispatch

'on':
  push:
    branches:
      - automation/dispatch-ms-20260923-home-functions-gateway-cors-r3

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    name: Dispatch protected release gate
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Dispatch production promotion as GitHub Actions automation
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api \
            --method POST \
            "repos/${GITHUB_REPOSITORY}/actions/workflows/mission-spine-promote.yml/dispatches" \
            --input - <<'JSON'
          {
            "ref": "main",
            "inputs": {
              "release_id": "ms-20260923-home-functions-gateway-cors-r3"
            }
          }
          JSON
