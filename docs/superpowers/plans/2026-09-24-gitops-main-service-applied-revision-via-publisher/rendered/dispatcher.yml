---
# Dispatch nonce: service applied revision main publisher attempt 1.
name: Mission Spine Release Gate Dispatch

'on':
  push:
    branches:
      - automation/dispatch-service-applied-revision-main-publish

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    name: Dispatch protected release gate
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Dispatch the service applied revision main publisher as GitHub Actions automation
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api \
            --method POST \
            "repos/${GITHUB_REPOSITORY}/actions/workflows/mission-spine-auth-smoke.yml/dispatches" \
            --input - <<'JSON'
          {
            "ref": "chore/service-applied-revision-publish-20260924",
            "inputs": {
              "full": "true"
            }
          }
          JSON
