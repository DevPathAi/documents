# SDD ledger — plan: C:/Users/deepe/AppData/Local/Temp/claude/D--workspace-dpa/9cb80789-bf71-49bb-8e11-bbc8f847c922/scratchpad/2026-09-24-s3-p1-semantic-tokens-2-0-0.md
Spec: documents docs/superpowers/specs/2026-09-19-web-native-redesign-and-mobile-split-design.md (read at setup); mock-up https://claude.ai/artifact/DWi8kMV6QcAzBEQwbrNPNd
Execution: Native (user, 2026-09-24). Home mirror: option A (user).
Pre-flight (shared interfaces):
- T1 -> T3/T4: DpDensity.controlHeight/rowPadding/minTarget, AppTokens.headerHeight, DpRadius.* — names identical in producer and consumers; clean
- T2 -> T3/T9: DpSemanticColorRole.header{Bg,Text,Muted,Faint,Active,Border} -> CSS --dp-color-header-* via _kebabCase; T9 test keys match; clean
- T3 -> T7/T9: --dp-density-control-height|row-padding|min-target, --dp-layout-header-height, version "2.0.0"; T9 dimension list uses the same kebab names; clean
- T4 -> T5: MIN_TARGET 24 == DpDensity.minTarget; clean
- Conflict found: T1 changes DpRadius but dp_semantic_tokens_test.dart:254-255 pins '999px'/'18px' literally, so T1 Step 5 full suite would fail before T3 rewrites that test. Ruling: in T1 update those two assertions to '4px'/'8px' (T3 restates them) — cost if wrong: none (T3 rewrites the block).
Setup: Ruling: melos bootstrap needs the CI-pinned Flutter first on PATH (export PATH=/d/workspace/dpa/.worktrees/_tools/flutter-3.44.1/bin:$PATH) — plain 'dart run melos bootstrap' resolved with Flutter 3.47.2 from PATH and refused the lockfile (7 SDK-pinned deps) — cost if wrong: none once PATH is set.
Task 1: complete (commits de316b4..19e790a, tests: flutter test --exclude-tags golden → 00:12 +250: All tests passed!)
Task 2: complete (commits 19e790a..83f7791, tests: flutter test --exclude-tags golden → 00:12 +251: All tests passed!)
Task 2: Ruling: sed \b missed 15 prose mentions followed by Hangul (test titles, comments) — completed the rename with a python regex so no rail* colour name remains; test titles now say header* — cost if wrong: none (prose only)
Task 3: complete (commits 83f7791..c5ca292, tests: flutter test --exclude-tags golden → 00:13 +251: All tests passed!)
Task 4: Ruling: dropped iconButtonTheme.fixedSize (kept minimumSize 30x30) — fixedSize forced every IconButton child into a 24x24 box after the 3px padding, and the flutter_quill toolbar's composite icon children overflowed by 80px in 42 assertions across three apps/web community tests; minimumSize alone satisfies the 30px contract and the density test still measures exactly 30x30 — cost if wrong: an icon button with an unusually wide child can grow past 30px in width (height stays 30).
Task 4: Ruling: added visualDensity: VisualDensity.compact to segmentedButtonTheme — SegmentedButton ignores minimumSize and computes its own floor (segmented_button.dart: textButtonMinHeight 40 + visualDensity.baseSizeAdjustment.dy), so 40px was unreachable from minimumSize; compact (dy -8) lowers it to 32px, inside the plan's [30, 34] band — cost if wrong: segments sit 2px taller than the other 30px controls.
Task 4: complete (commits c5ca292..9ac3b7c, tests: flutter test --exclude-tags golden → 00:11 +255: All tests passed!)
Task 5: Ruling: also amended the 2026-09-17 findings table row whose 조치 cell prescribed "테마 표준 밀도, 세그먼트 padded 탭 타깃" — that remedy is the one contract 2.0.0 reverses, and a reader following the historical row would undo Task 4; appended the superseding note instead of rewriting the record — cost if wrong: one extra clause in a history table.
Task 5: complete (commits 9ac3b7c..2c89ab1, tests: node --test tools/browser_ux/run.test.mjs → ℹ duration_ms 210.2813)
Task 6: complete (commits 2c89ab1..bbbd086, tests: flutter test --exclude-tags golden → 00:14 +255: All tests passed!)
Task 6: Ruling: also rewrote DESIGN.md §1's contract paragraph (it still called the manifest v1.1.0 and told the landing mirror to follow that version) — the plan's Task 6 named §3/§5/§6 only, but leaving §1 would have the SSoT section contradict the manifest this plan bumps; recorded as an addendum commit after the Task 6 ledger line — cost if wrong: one extra doc commit inside the same task's range.
Task 6: addendum commit 681d102 (DESIGN.md §1)
Task 7: Ruling: uploaded the 8 text files only, not the 5 woff2 binaries — build_ds_bundle.py resolves fonts from the unchanged .design-sync/fonts.json with sha256 verification, so the binaries are byte-identical to the 2026-09-17 sync; the tool's own guidance is to sync incrementally rather than wholesale-replace — cost if wrong: a font file on the remote stays at the previous (identical) bytes.
Task 7: complete (commits bbbd0860..390578c, tests: flutter test --exclude-tags golden → 00:12 +255: All tests passed!)
Task 8: Ruling: fixed three analyzer lints in the test code the plan supplied verbatim (unnecessary_import + sort_child_properties_last in dp_tap_target_test, unnecessary_cast in dp_radius_density_test — AppTokens.lerp already returns AppTokens) — CI runs `melos run analyze` as a gate, so the plan's literal test code could not merge as written — cost if wrong: none, behaviour of the tests is unchanged.
Task 9: Ruling: did not run the visual baseline update (plan Step 5) — the Docker daemon is not running on this machine and the repo's standing rule (2026-09-19 handoff §9) is that Playwright work runs in the pinned image, not ad-hoc locally while the user is on the machine; opened the home PR as a DRAFT that states the visual-a11y job will fail until baselines are re-recorded and approved, so nothing can merge by accident — cost if wrong: the home mirror waits for one Docker run; the landing keeps contract 1.1.0 in production meanwhile, which is the documented divergence.
Task 9: Ruling: left the landing's own "44x44 hit target" accessibility rule in devpath-home-page/DESIGN.md untouched — option A was a decision about mirroring token VALUES; the landing's a11y baseline is a landing design decision and spec §3 puts landing design out of scope — cost if wrong: the landing keeps a stricter target than the app, which is never a violation.
Task 9: home repo commit 44b8212 on feat/tokens-mirror-2-0-0, draft PR DevPathAi/devpath-home-page#93 (the completion line below records the frontend worktree range, which does not move for this task)
Task 9: BLOCKED, not complete — `npm test` in the home worktree fails one guard (tests/visual-evidence-audit-contract.test.js "allows evidence-only and non-rendering descendants…"): it diffs the rendered-product commit against HEAD and requires every changed path to be evidence-only, so changing assets/tokens.css, index.html, assets/styles.css and assets/public.css without re-binding the render baseline is exactly what it is built to catch. Completing it needs the pinned Playwright image (visual:evidence:docker + visual:baseline:update) and a human baseline approval. Code+tests are committed on feat/tokens-mirror-2-0-0 (44b8212) and parked in DRAFT PR devpath-home-page#93.
Task 8: Ruling: deferred the merge (plan Step 4) until after the whole-branch review — the plan put the merge inside Task 8, before Task 9 and before the review, but a finding fixed after a merge needs a second PR; CI is also still running (perf-gate, web-image-config-contract, produce-atomic-pair) — cost if wrong: the branch sits open for one review cycle.
Task 8: Ruling: will NOT remove the frontend worktree at merge time (plan Step 4 last line) — this ledger and the review package live inside it — cost if wrong: one worktree to clean up at the end.
